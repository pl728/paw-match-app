server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    # Send all routes to index.html so React Router handles them
    location / {
        try_files $uri $uri/ /index.html;
    }
}
